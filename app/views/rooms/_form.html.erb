
<%= form_for(@room) do |f| %>
<%# form_for :room, :url => { :action => "create" } do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label t("room.hash_tag") %><br />
    <%= f.text_field :hash_tag, :class => "input_field_width_300" %><br />
  </div>
  
  <br />
  <br />

  <div>
    <%= f.label t("room.site") %><br />
    <%= f.text_field :site, :class => "input_field_width_300" %><br />
  </div>

  <br />
  <br />

  <div>
    <%= f.submit t("menu.#{@submit}"), :disable_with => t("menu.#{@submit}") %>
    <br />
  </div>
<% end %>
